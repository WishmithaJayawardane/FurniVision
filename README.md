# FurniVision

This is a NextJS starter in Firebase Studio.

To run the app:
1.run command npm.cmd run dev to start the development server.
2.open http://localhost:9002 in your browser to view the app.

To build the app:
run command npm.cmd run build